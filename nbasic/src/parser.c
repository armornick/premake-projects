/*
 * Generated by the Waxeye Parser Generator - version 0.8.0
 * www.waxeye.org
 */

#define PARSER_C_
#include "parser.h"
#include <assert.h>

const char *type_strings[] = {
    "program",
    "statement",
    "endstat",
    "inputstat",
    "printstat",
    "ifstat",
    "thenclause",
    "elseclause",
    "whilestat",
    "assignstat",
    "dostat",
    "cond",
    "exprlist",
    "expression",
    "term",
    "factor",
    "END",
    "PRINT",
    "INPUT",
    "IF",
    "THEN",
    "ELSE",
    "WHILE",
    "WEND",
    "LET",
    "DO",
    "ID",
    "NUM",
    "STRING",
    "COMMA",
    "LPAREN",
    "RPAREN",
    "ADDOP",
    "MULTOP",
    "RELOP",
    "ASSIGN",
    "endl",
    "ws"
};

struct parser_t* parser_new() {
    const size_t start = 0;
    const bool eof_check = true;
    char *single;
    char *min;
    char *max;
    size_t num_single;
    size_t num_range;
    size_t num_edges;
    size_t num_states;
    const size_t num_automata = 42;
    struct trans_t trans;
    union trans_data trans_d;
    struct edge_t *edges;
    struct state_t *states;
    struct fa_t *automata = calloc(num_automata, sizeof(struct fa_t));
    assert(automata != NULL);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 2, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 0, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[0], MODE_LEFT, TYPE_PROGRAM, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 7;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 2;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    trans_d.fa = 3;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 2, false);
    trans_d.fa = 4;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[2], trans, 2, false);
    trans_d.fa = 5;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[3], trans, 2, false);
    trans_d.fa = 8;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[4], trans, 2, false);
    trans_d.fa = 9;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[5], trans, 2, false);
    trans_d.fa = 10;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[6], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[1], MODE_LEFT, TYPE_STATEMENT, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 16;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 15;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, true);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[2], MODE_LEFT, TYPE_ENDSTAT, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 18;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 28;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    trans_d.fa = 26;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 4, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 29;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, true);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 26;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[3], MODE_LEFT, TYPE_INPUTSTAT, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 17;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 12;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[4], MODE_LEFT, TYPE_PRINTSTAT, states, num_states);

    num_states = 15;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 11;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 20;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, true);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 39;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 9, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 21;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, true);
    state_init(&states[5], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 38;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 8, false);
    state_init(&states[7], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[8], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 6;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 10, false);
    state_init(&states[9], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 21;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 11, true);
    trans_d.fa = 16;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 14, true);
    state_init(&states[10], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 12, false);
    state_init(&states[11], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 7;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 13, false);
    state_init(&states[12], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 16;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 14, true);
    state_init(&states[13], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 19;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 8, true);
    state_init(&states[14], edges, num_edges, false);
    fa_init(&automata[5], MODE_LEFT, TYPE_IFSTAT, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[3], edges, num_edges, false);
    fa_init(&automata[6], MODE_LEFT, TYPE_THENCLAUSE, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[3], edges, num_edges, false);
    fa_init(&automata[7], MODE_LEFT, TYPE_ELSECLAUSE, states, num_states);

    num_states = 8;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 22;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 11;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    trans_d.fa = 23;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 7, true);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[7], edges, num_edges, true);
    fa_init(&automata[8], MODE_LEFT, TYPE_WHILESTAT, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 24;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    trans_d.fa = 26;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 2, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 26;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 35;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, true);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[9], MODE_LEFT, TYPE_ASSIGNSTAT, states, num_states);

    num_states = 8;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 25;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 1;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    trans_d.fa = 22;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 6, true);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 36;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 11;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 7, false);
    state_init(&states[6], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[7], edges, num_edges, true);
    fa_init(&automata[10], MODE_LEFT, TYPE_DOSTAT, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 34;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[11], MODE_LEFT, TYPE_COND, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 28;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 29;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, true);
    state_init(&states[1], edges, num_edges, true);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 28;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[2], edges, num_edges, false);
    fa_init(&automata[12], MODE_LEFT, TYPE_EXPRLIST, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 32;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 14;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 2, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 14;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 32;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 14;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[3], edges, num_edges, false);
    fa_init(&automata[13], MODE_LEFT, TYPE_EXPRESSION, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 15;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 33;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 15;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[2], edges, num_edges, false);
    fa_init(&automata[14], MODE_LEFT, TYPE_TERM, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 3;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 26;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 27;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 1, false);
    trans_d.fa = 30;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[2], trans, 2, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 13;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 31;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[3], edges, num_edges, false);
    fa_init(&automata[15], MODE_LEFT, TYPE_FACTOR, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'E';
    single[1] = 'e';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'N';
    single[1] = 'n';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'D';
    single[1] = 'd';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[16], MODE_LEFT, TYPE_END, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'P';
    single[1] = 'p';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'R';
    single[1] = 'r';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'I';
    single[1] = 'i';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'N';
    single[1] = 'n';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'T';
    single[1] = 't';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[17], MODE_LEFT, TYPE_PRINT, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'I';
    single[1] = 'i';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'N';
    single[1] = 'n';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'P';
    single[1] = 'p';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'U';
    single[1] = 'u';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'T';
    single[1] = 't';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[18], MODE_LEFT, TYPE_INPUT, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'I';
    single[1] = 'i';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'F';
    single[1] = 'f';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[19], MODE_LEFT, TYPE_IF, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'T';
    single[1] = 't';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'H';
    single[1] = 'h';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'E';
    single[1] = 'e';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'N';
    single[1] = 'n';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[20], MODE_LEFT, TYPE_THEN, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'E';
    single[1] = 'e';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'L';
    single[1] = 'l';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'S';
    single[1] = 's';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'E';
    single[1] = 'e';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[21], MODE_LEFT, TYPE_ELSE, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'W';
    single[1] = 'w';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'H';
    single[1] = 'h';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'I';
    single[1] = 'i';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'L';
    single[1] = 'l';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'E';
    single[1] = 'e';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[22], MODE_LEFT, TYPE_WHILE, states, num_states);

    num_states = 6;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'W';
    single[1] = 'w';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'E';
    single[1] = 'e';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'N';
    single[1] = 'n';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'D';
    single[1] = 'd';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 5, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[5], edges, num_edges, true);
    fa_init(&automata[23], MODE_LEFT, TYPE_WEND, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'L';
    single[1] = 'l';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'E';
    single[1] = 'e';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'T';
    single[1] = 't';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[4], edges, num_edges, true);
    fa_init(&automata[24], MODE_LEFT, TYPE_LET, states, num_states);

    num_states = 4;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'D';
    single[1] = 'd';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = 'O';
    single[1] = 'o';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[3], edges, num_edges, true);
    fa_init(&automata[25], MODE_LEFT, TYPE_DO, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 2;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '_';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = 'A';
    min[1] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = 'Z';
    max[1] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 1;
    num_range = 2;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '-';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '0';
    min[1] = 'a';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '_';
    max[1] = 'z';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[26], MODE_LEFT, TYPE_ID, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 0;
    num_range = 1;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '1';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '9';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 0;
    num_range = 1;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    min[0] = '0';
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    max[0] = '9';
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[1], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[27], MODE_LEFT, TYPE_NUM, states, num_states);

    num_states = 7;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '"';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, true);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 41;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[2], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 40;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 4, false);
    trans_d.c = '"';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 5, true);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '\0';
    trans_init(&trans, TRANS_WILD, trans_d);
    edge_init(&edges[0], trans, 3, false);
    state_init(&states[4], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 6, false);
    state_init(&states[5], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[6], edges, num_edges, true);
    fa_init(&automata[28], MODE_LEFT, TYPE_STRING, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = ',';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[29], MODE_LEFT, TYPE_COMMA, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '(';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[30], MODE_LEFT, TYPE_LPAREN, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = ')';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[31], MODE_LEFT, TYPE_RPAREN, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '+';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.c = '-';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[32], MODE_LEFT, TYPE_ADDOP, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '*';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.c = '/';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[33], MODE_LEFT, TYPE_MULTOP, states, num_states);

    num_states = 5;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 4;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '=';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.c = '<';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 1, false);
    trans_d.c = '>';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[2], trans, 3, false);
    trans_d.c = '<';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[3], trans, 4, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    trans_d.c = '=';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[3], edges, num_edges, false);
    num_edges = 2;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '=';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    trans_d.c = '>';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[1], trans, 1, false);
    state_init(&states[4], edges, num_edges, false);
    fa_init(&automata[34], MODE_LEFT, TYPE_RELOP, states, num_states);

    num_states = 3;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.c = '=';
    trans_init(&trans, TRANS_CHAR, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 37;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 2, false);
    state_init(&states[1], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[2], edges, num_edges, true);
    fa_init(&automata[35], MODE_LEFT, TYPE_ASSIGN, states, num_states);

    num_states = 1;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\n';
    single[1] = '\r';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 0, false);
    state_init(&states[0], edges, num_edges, true);
    fa_init(&automata[36], MODE_VOID, TYPE_ENDL, states, num_states);

    num_states = 1;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 2;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\t';
    single[1] = ' ';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 0, false);
    state_init(&states[0], edges, num_edges, true);
    fa_init(&automata[37], MODE_VOID, TYPE_WS, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 5;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[38], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    trans_d.fa = 5;
    trans_init(&trans, TRANS_FA, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[39], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 3;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\n';
    single[1] = '\r';
    single[2] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[40], MODE_NEG, 0, states, num_states);

    num_states = 2;
    states = calloc(num_states, sizeof(struct state_t));
    assert(states != NULL);
    num_edges = 1;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    num_single = 3;
    num_range = 0;
    single = calloc(num_single, sizeof(char));
    assert(single != NULL);
    single[0] = '\n';
    single[1] = '\r';
    single[2] = '"';
    min = calloc(num_range, sizeof(char));
    assert(min != NULL);
    max = calloc(num_range, sizeof(char));
    assert(max != NULL);
    trans_d.set = set_new(single, num_single, min, max, num_range);
    trans_init(&trans, TRANS_SET, trans_d);
    edge_init(&edges[0], trans, 1, false);
    state_init(&states[0], edges, num_edges, false);
    num_edges = 0;
    edges = calloc(num_edges, sizeof(struct edge_t));
    assert(edges != NULL);
    state_init(&states[1], edges, num_edges, true);
    fa_init(&automata[41], MODE_NEG, 0, states, num_states);

    return wparser_new(start, automata, num_automata, eof_check);
}
